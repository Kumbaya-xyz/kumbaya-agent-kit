# Token -> key map (or use SIGNER_KEYS_FILE for a JSON file).
# { "<token>": { "key": "0x..", "label": "..", "policy": { "allowChains": [6343], "maxValueWei": "..", "allowTo": ["0x.."] } } }
SIGNER_KEYS=
# SIGNER_KEYS_FILE=./keys.json

# Listen port.
PORT=8787
